# Server-side only — used by api/propose-cohort.js. Never exposed to the client.
ANTHROPIC_API_KEY=sk-ant-...

# Client-side (Vite inlines VITE_* at build time). The Zeffy donation URL.
VITE_ZEFFY_URL=https://www.zeffy.com/en-US/donation-form/you-give-learners-never-have-to-pay
